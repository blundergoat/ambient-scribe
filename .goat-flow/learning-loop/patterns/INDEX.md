---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Thin vertical slice on contract changes](architecture.md) (search: "## Pattern: Thin vertical slice on contract changes") - A change crosses PHP, Python, and browser surfaces, such as a new Mercure topic, WebSocket frame type, role payload field, or session config value.
- [ThreadPoolExecutor for all NeMo calls](architecture.md) (search: "## Pattern: ThreadPoolExecutor for all NeMo calls") - NeMo inference is synchronous GPU work inside an async FastAPI process.
- [Mercure topic per concern, not per session](architecture.md) (search: "## Pattern: Mercure topic per concern, not per session") - Raw transcription, role attribution, and summary generation have different latency and failure profiles.
- [Collapse removed selectors to constants](architecture.md) (search: "## Pattern: Collapse removed selectors to constants") - A user-facing selector is removed and only one behavior remains, such as the 0.3.0 collapse from multiple scribe profiles to medical-only transcription.
- [Verify Twig without assuming `bin/console`](verification.md) (search: "## Pattern: Verify Twig without assuming `bin/console`") - This checkout has Symfony components and PHPUnit coverage, but no `bin/console` entrypoint.
- [Configure generated references out of gruff cleanup](verification.md) (search: "## Pattern: Configure generated references out of gruff cleanup") - `config/reference.php` identifies itself as auto-generated, but gruff-php can still report docs or waste findings there during full-project scans.
- [Keep fixture evals on lightweight domain helpers](verification.md) (search: "## Pattern: Keep fixture evals on lightweight domain helpers") - A local quality script needs to score fixture behavior, such as DOCTOR/PATIENT role attribution, without starting FastAPI, importing HTTP clients, or touching NeMo/GPU setup.
