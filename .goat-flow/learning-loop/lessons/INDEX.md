---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Question misclassified as directive (2026-03-21)](agent-behavior.md) (search: "## Lesson: Question misclassified as directive (2026-03-21)") - "How does session cleanup work?" was treated as a directive to implement changes to session cleanup.
- [Gitignored plan intake needs directory listing (2026-07-05)](agent-behavior.md) (search: "## Lesson: Gitignored plan intake needs directory listing (2026-07-05)") - During a Copilot harness repair, `rg --files .goat-flow/plans` only showed tracked files and missed existing gitignored milestone directories.
- [Harness line-count failures after instruction edits (2026-07-04)](review-feedback.md) (search: "## Lesson: Harness line-count failures after instruction edits (2026-07-04)") - Adding required hot-path headings to `CLAUDE.md` fixed structural checks but pushed the file over the harness hard limit reported by `instruction-line-count`.
- [Graceful client recovery can disguise a server crash as a clean stop (2026-07-05)](verification.md) (search: "## Lesson: Graceful client recovery can disguise a server crash as a clean stop (2026-07-05)") - A demo replay "stopped by itself" at ~70s and was signed off as the socket-drop recovery path working: the browser drained, showed "Replay stopped", and even produced good ground-truth metrics.
- [Config-echo checkmarks are not integration checks - invoke the dependency from its runtime (2026-07-05)](verification.md) (search: "## Lesson: Config-echo checkmarks are not integration checks - invoke the dependency from its runtime (2026-07-05)") - `./scripts/check-ai-model.sh` printed `✔ model us.anthropic.claude-haiku-4-5-20251001-v1:0` and `✔ AWS credentials resolve with STS`, so Bedrock looked healthy while every summary failed in ~140ms…
- [Guard shared socket handlers against stale-socket events (2026-07-05)](verification.md) (search: "## Lesson: Guard shared socket handlers against stale-socket events (2026-07-05)") - While converting demo replay to stream over the live WebSocket, the shared `onclose` handler gained a replay branch (`public/js/scribe-recording.js`, search: "A drop of the replay's own socket").
- [Verify the running container's env, not the compose default (2026-07-04)](verification.md) (search: "## Lesson: Verify the running container's env, not the compose default (2026-07-04)") - After changing the `docker-compose.yml` default to `OLLAMA_HOST=http://ollama:11434`, one `docker compose up -d` showed the agent with the fixed value, so the "agent can't reach Ollama" bug was…
- [Audio format mismatch - read both pipeline ends (2026-03-21)](verification.md) (search: "## Lesson: Audio format mismatch - read both pipeline ends (2026-03-21)") - AudioBuffer in `strands_agents/nemo_session.py` assumed 16 kHz 16-bit PCM while the browser MediaRecorder sent WebM/Opus.
- [Stale references after rename (2026-03-21)](verification.md) (search: "## Lesson: Stale references after rename (2026-03-21)") - After renaming `mercure_topic_raw` to `mercure_topic_segments`, stale references remained in config and docs.
- [Browser stream state ordering needs a focused regression check (2026-07-04)](verification.md) (search: "## Lesson: Browser stream state ordering needs a focused regression check (2026-07-04)") - git history (auto-seeded) Evidence: `templates/scribe/index.html.twig` + commit `0125a6b` fixed the StreamOrchestrator `_active` ordering bug.
- [Live transcription fixes need cross-layer verification, not one-service checks (2026-07-04)](verification.md) (search: "## Lesson: Live transcription fixes need cross-layer verification, not one-service checks (2026-07-04)") - git history (auto-seeded) Evidence: `docker-compose.yml`, `strands_agents/api/server.py`, `strands_agents/nemo_pipeline.py`, `strands_agents/nemo_session.py`, `templates/scribe/index.html.twig`, and…
- [Semantic anchors should prefer function names over escaped route strings (2026-07-04)](verification.md) (search: "## Lesson: Semantic anchors should prefer function names over escaped route strings (2026-07-04)") - `./scripts/context-validate.sh` rejected a generated footgun citation that used an escaped decorator string for the WebSocket route in `strands_agents/api/server.py`.
- [Goat-flow installed skill edits can drift from package templates (2026-07-04)](verification.md) (search: "## Lesson: Goat-flow installed skill edits can drift from package templates (2026-07-04)") - Updating installed goat-plan skill copies under `.agents/skills/`, `.claude/skills/`, and `.github/skills/` removed stale project path text, but `goat-flow audit` still compares those files to the…
- [Gruff context docs need marker vocabulary (2026-07-04)](verification.md) (search: "## Lesson: Gruff context docs need marker vocabulary (2026-07-04)") - During M05, comments clearly described user-visible error handling but still failed `docs.missing-error-behavior-doc` because gruff's context-doc rule looks for marker words such as `reports`,…
- [Gruff env placeholders are exact-token sensitive (2026-07-04)](verification.md) (search: "## Lesson: Gruff env placeholders are exact-token sensitive (2026-07-04)") - During M06, intuitive placeholders such as `<generate-app-secret>` and `allowlists.secretPreviews` still left `sensitive-data.hardcoded-env-value` findings.
- [Gruff PHP display filters do not lower the exit threshold (2026-07-04)](verification.md) (search: "## Lesson: Gruff PHP display filters do not lower the exit threshold (2026-07-04)") - During M07, a complexity-only gruff-php command was initially considered for the retired cyclomatic alias.
- [Generic Gruff baseline filenames collide across tool lanes (2026-07-05)](verification.md) (search: "## Lesson: Generic Gruff baseline filenames collide across tool lanes (2026-07-05)") - During M14 review, a PHP accepted-debt baseline was written as `gruff-baseline.json`.
- [Validation wrappers must check exit codes before success text (2026-07-05)](verification.md) (search: "## Lesson: Validation wrappers must check exit codes before success text (2026-07-05)") - During M14 review, direct `composer validate --strict` exited 1 for the intentionally commit-pinned Strands PHP client, but preflight had been grepping for "is valid" and therefore reported the step…
- [SDK observability plans must match installed vendor contracts (2026-07-04)](verification.md) (search: "## Lesson: SDK observability plans must match installed vendor contracts (2026-07-04)") - During M08, the plan described PHP client 1.5.x `ResponseObserver` hooks, but the installed 1.4.0 client only exposes `RequestMiddleware` with `beforeRequest()` and `afterResponse()`.
- [Goat-flow setup-green can still hide cross-agent drift (2026-07-05)](verification.md) (search: "## Lesson: Goat-flow setup-green can still hide cross-agent drift (2026-07-05)") - During a Codex goat-flow 1.13.1 repair, `goat-flow setup . --agent codex` reported `0 audit checks failed` after codex config, hooks, and skills were synced.
- [Logging-only edits can still break hot role logic (2026-07-05)](verification.md) (search: "## Lesson: Logging-only edits can still break hot role logic (2026-07-05)") - During M18 Phase 0, a plain-log message was added to `RoleMappingState.did_update_mapping_detect_flip()` using `changed_speakers`, but that variable existed only inside `_is_role_label_flip()`.
- [Eval history fetch must wait out post-disconnect role churn (2026-07-05)](verification.md) (search: "## Lesson: Eval history fetch must wait out post-disconnect role churn (2026-07-05)") - During M20 Phase 1, three c03 @83s runs after a publish-only payload change all scored 45.0% strict attribution against a 55.0% Phase 0 median, which looked like the kill criterion firing on the new…
- [Free-assignment oracle metrics overstate reachable accuracy - constrain the assignment (2026-07-05)](verification.md) (search: "## Lesson: Free-assignment oracle metrics overstate reachable accuracy - constrain the assignment (2026-07-05)") - The M16 "speaker oracle accuracy" gave each emitted speaker ID its majority reference role independently, so on fixtures where diarization mixed one voice across BOTH IDs the oracle assigned DOCTOR…
- [Prompt-only role establishment hints need median replay proof (2026-07-05)](verification.md) (search: "## Lesson: Prompt-only role establishment hints need median replay proof (2026-07-05)") - During M20 Phase 4, removing the role agent's `current_mapping` echo and adding cue-rich representative rows looked mechanically correct, but c03 @83s immediately scored 45/45 strict on two runs.
- [Region WER buckets need word-level allocation, not whole-interval exclusion (2026-07-05)](verification.md) (search: "## Lesson: Region WER buckets need word-level allocation, not whole-interval exclusion (2026-07-05)") - During M17 Phase 0, the first clean-vs-overlap WER split assigned an entire TextGrid interval or transcript row to the overlap bucket if it touched cross-talk at all.
- [Windowed-emission tests must clear the minimum-new-audio floor (2026-07-05)](verification.md) (search: "## Lesson: Windowed-emission tests must clear the minimum-new-audio floor (2026-07-05)") - During M17 fragment-policy work, a regression test intended to prove alternating-speaker fragments stayed separate put the first fragment's end exactly at the `_MIN_NEW_AUDIO_SECONDS` boundary.
- [Emission floors need inclusive boundary tests (2026-07-05)](verification.md) (search: "## Lesson: Emission floors need inclusive boundary tests (2026-07-05)") - During M17 seam fallback work, raising the context-replay floor from 0.3s to 0.5s used a strict `>` comparison.
- [Log-derived timelines must count state changes, not echoed decisions (2026-07-05)](verification.md) (search: "## Lesson: Log-derived timelines must count state changes, not echoed decisions (2026-07-05)") - During M16 diagnostics, the first role-timeline summary counted accepted flips whenever a timeline row had `decision == "accepted_flip"`.
- [Original fixture metrics outrank plausible seam fixes (2026-07-05)](verification.md) (search: "## Lesson: Original fixture metrics outrank plausible seam fixes (2026-07-05)") - A conservative speaker-anchor change for consultation-03 passed the focused `tests/python/test_nemo_session.py` suite and matched the local theory that tiny context-tail overlaps can trigger bad…
- [Held-tail speaker anchors need replay proof before adoption (2026-07-05)](verification.md) (search: "## Lesson: Held-tail speaker anchors need replay proof before adoption (2026-07-05)") - During M20 Phase 5, the per-window artifacts made held-tail speaker anchoring look like the small seam mechanism M16 had left open: c03 @83s had 18 held rows and wrong rows clustered at window…
- [Dataclass script imports need sys.modules registration (2026-07-04)](verification.md) (search: "## Lesson: Dataclass script imports need sys.modules registration (2026-07-04)") - Full pytest caught that the test helper loaded `scripts/analyze-logs.py` with `importlib.util.module_from_spec()` but did not register it in `sys.modules` before executing the module.
- [GPU image import gates need a local/pending split (2026-07-04)](verification.md) (search: "## Lesson: GPU image import gates need a local/pending split (2026-07-04)") - During M09, the plan required a `docker run nvcr.io/nvidia/nemo:26.02 ...` import check before dependency edits.
- [Name GPU-pending fallbacks as fallbacks (2026-07-04)](verification.md) (search: "## Lesson: Name GPU-pending fallbacks as fallbacks (2026-07-04)") - During M11, the plan targeted NeMo decode-time phrase boosting, but the exact multitalker transducer API still needs a pinned-container GPU spike.
- [Hidden sidebars need layout-state smoke tests (2026-07-04)](verification.md) (search: "## Lesson: Hidden sidebars need layout-state smoke tests (2026-07-04)") - During M12, the first clinical-hints UI pass hid the sidebar element but left a dedicated desktop grid column in the base layout.
- [Optional JSON knowledge files need malformed-file coverage (2026-07-04)](verification.md) (search: "## Lesson: Optional JSON knowledge files need malformed-file coverage (2026-07-04)") - During M12, the clinical KB loader handled missing files and invalid shapes but did not handle malformed JSON.
- [Model stack docs need env, Compose, and code defaults checked together (2026-07-04)](verification.md) (search: "## Lesson: Model stack docs need env, Compose, and code defaults checked together (2026-07-04)") - While creating the stack inventory, the root README still described Bedrock as the role-inference default, `.env.example` described Ollama as the local default, Compose passed Ollama by default, and…
- [Browser replay smokes need a trustworthy origin (2026-07-04)](verification.md) (search: "## Lesson: Browser replay smokes need a trustworthy origin (2026-07-04)") - While testing the Demo Audio picker, a Playwright smoke loaded the page on `http://app.test/`.
- [Populated transcript layouts need populated browser smokes (2026-07-04)](verification.md) (search: "## Lesson: Populated transcript layouts need populated browser smokes (2026-07-04)") - During the 0.3.0 mockup refresh, an empty-state screenshot made the new workspace layout look clean, but a populated transcript/summary browser smoke exposed that direct transcript events could leave…
- [Eval-generated session IDs must use route-valid UUIDs and avoid retry collisions (2026-07-05)](verification.md) (search: "## Lesson: Eval-generated session IDs must use route-valid UUIDs and avoid retry collisions (2026-07-05)") - During M17 channel-ceiling work, the first eval script posted human-readable session IDs such as `primock57-...-doctor` to `/transcribe/file`; FastAPI rejected them with `400 Bad Request` because the…
