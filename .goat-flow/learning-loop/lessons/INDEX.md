---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Audio format mismatch — read both pipeline ends (2026-03-21)](verification.md) (search: "## Lesson: Audio format mismatch — read both pipeline ends (2026-03-21)") - AudioBuffer in `strands_agents/nemo_session.py` assumed 16 kHz 16-bit PCM while the browser MediaRecorder sent WebM/Opus.
- [Question misclassified as directive (2026-03-21)](verification.md) (search: "## Lesson: Question misclassified as directive (2026-03-21)") - "How does session cleanup work?" was treated as a directive to implement changes to session cleanup.
- [Stale references after rename (2026-03-21)](verification.md) (search: "## Lesson: Stale references after rename (2026-03-21)") - After renaming `mercure_topic_raw` to `mercure_topic_segments`, stale references remained in config and docs.
- [Harness line-count failures after instruction edits (2026-07-04)](verification.md) (search: "## Lesson: Harness line-count failures after instruction edits (2026-07-04)") - Adding required hot-path headings to `CLAUDE.md` fixed structural checks but pushed the file over the harness hard limit reported by `instruction-line-count`.
- [Browser stream state ordering needs a focused regression check (2026-07-04)](verification.md) (search: "## Lesson: Browser stream state ordering needs a focused regression check (2026-07-04)") - git history (auto-seeded) Evidence: `templates/scribe/index.html.twig` + commit `0125a6b` fixed the StreamOrchestrator `_active` ordering bug.
- [Live transcription fixes need cross-layer verification, not one-service checks (2026-07-04)](verification.md) (search: "## Lesson: Live transcription fixes need cross-layer verification, not one-service checks (2026-07-04)") - git history (auto-seeded) Evidence: `docker-compose.yml`, `strands_agents/api/server.py`, `strands_agents/nemo_pipeline.py`, `strands_agents/nemo_session.py`, `templates/scribe/index.html.twig`, and…
- [Semantic anchors should prefer function names over escaped route strings (2026-07-04)](verification.md) (search: "## Lesson: Semantic anchors should prefer function names over escaped route strings (2026-07-04)") - `./scripts/context-validate.sh` rejected a generated footgun citation that used an escaped decorator string for the WebSocket route in `strands_agents/api/server.py`.
- [Goat-flow installed skill edits can drift from package templates (2026-07-04)](verification.md) (search: "## Lesson: Goat-flow installed skill edits can drift from package templates (2026-07-04)") - Updating installed goat-plan skill copies under `.agents/skills/`, `.claude/skills/`, and `.github/skills/` removed stale project path text, but `goat-flow audit` still compares those files to the…
- [Gruff context docs need marker vocabulary (2026-07-04)](verification.md) (search: "## Lesson: Gruff context docs need marker vocabulary (2026-07-04)") - During M05, comments clearly described user-visible error handling but still failed `docs.missing-error-behavior-doc` because gruff's context-doc rule looks for marker words such as `reports`,…
- [Gruff env placeholders are exact-token sensitive (2026-07-04)](verification.md) (search: "## Lesson: Gruff env placeholders are exact-token sensitive (2026-07-04)") - During M06, intuitive placeholders such as `<generate-app-secret>` and `allowlists.secretPreviews` still left `sensitive-data.hardcoded-env-value` findings.
- [Gruff PHP display filters do not lower the exit threshold (2026-07-04)](verification.md) (search: "## Lesson: Gruff PHP display filters do not lower the exit threshold (2026-07-04)") - During M07, a complexity-only gruff-php command was initially considered for the retired cyclomatic alias.
- [SDK observability plans must match installed vendor contracts (2026-07-04)](verification.md) (search: "## Lesson: SDK observability plans must match installed vendor contracts (2026-07-04)") - During M08, the plan described PHP client 1.5.x `ResponseObserver` hooks, but the installed 1.4.0 client only exposes `RequestMiddleware` with `beforeRequest()` and `afterResponse()`.
- [Dataclass script imports need sys.modules registration (2026-07-04)](verification.md) (search: "## Lesson: Dataclass script imports need sys.modules registration (2026-07-04)") - Full pytest caught that the test helper loaded `scripts/analyze-logs.py` with `importlib.util.module_from_spec()` but did not register it in `sys.modules` before executing the module.
- [GPU image import gates need a local/pending split (2026-07-04)](verification.md) (search: "## Lesson: GPU image import gates need a local/pending split (2026-07-04)") - During M09, the plan required a `docker run nvcr.io/nvidia/nemo:26.02 ...` import check before dependency edits.
- [Name GPU-pending fallbacks as fallbacks (2026-07-04)](verification.md) (search: "## Lesson: Name GPU-pending fallbacks as fallbacks (2026-07-04)") - During M11, the plan targeted NeMo decode-time phrase boosting, but the exact multitalker transducer API still needs a pinned-container GPU spike.
- [Hidden sidebars need layout-state smoke tests (2026-07-04)](verification.md) (search: "## Lesson: Hidden sidebars need layout-state smoke tests (2026-07-04)") - During M12, the first clinical-hints UI pass hid the sidebar element but left a dedicated desktop grid column in the base layout.
- [Optional JSON knowledge files need malformed-file coverage (2026-07-04)](verification.md) (search: "## Lesson: Optional JSON knowledge files need malformed-file coverage (2026-07-04)") - During M12, the clinical KB loader handled missing files and invalid shapes but did not handle malformed JSON.
