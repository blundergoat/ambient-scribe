---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Question misclassified as directive (2026-03-21)](agent-behavior.md) (search: "## Lesson: Question misclassified as directive (2026-03-21)") - "How does session cleanup work?" was treated as a directive to implement changes to session cleanup.
- [Gitignored plan intake needs directory listing (2026-07-05)](agent-behavior.md) (search: "## Lesson: Gitignored plan intake needs directory listing (2026-07-05)") - During a Copilot harness repair, `rg --files .goat-flow/plans` only showed tracked files and missed existing gitignored milestone directories.
- [Appended tests inherit whatever class ends the file](agent-behavior.md) (search: "## Lesson: Appended tests inherit whatever class ends the file") - A hardening test appended to the end of `test_summary.py` via a shell heredoc landed inside the LAST class in the file, which lacked the `_strip` helper the test called - it failed with…
- [Strict attribution excludes cross-talk rows - check overlap-touch before predicting gate flips](eval-metrics.md) (search: "## Lesson: Strict attribution excludes cross-talk rows - check overlap-touch before predicting gate flips") - The M07 handoff predicted the consult-08 echo-split doctor row would swing strict attribution between 90.9% (pass) and 87.9% (fail).
- [Eval history fetch must wait out post-disconnect role churn (2026-07-05)](eval-metrics.md) (search: "## Lesson: Eval history fetch must wait out post-disconnect role churn (2026-07-05)") - During M20 Phase 1, three c03 @83s runs after a publish-only payload change all scored 45.0% strict attribution against a 55.0% Phase 0 median, which looked like the kill criterion firing on the new…
- [Free-assignment oracle metrics overstate reachable accuracy - constrain the assignment (2026-07-05)](eval-metrics.md) (search: "## Lesson: Free-assignment oracle metrics overstate reachable accuracy - constrain the assignment (2026-07-05)") - The M16 "speaker oracle accuracy" gave each emitted speaker ID its majority reference role independently, so on fixtures where diarization mixed one voice across BOTH IDs the oracle assigned DOCTOR…
- [Prompt-only role establishment hints need median replay proof (2026-07-05)](eval-metrics.md) (search: "## Lesson: Prompt-only role establishment hints need median replay proof (2026-07-05)") - During M20 Phase 4, removing the role agent's `current_mapping` echo and adding cue-rich representative rows looked mechanically correct, but c03 @83s immediately scored 45/45 strict on two runs.
- [Region WER buckets need word-level allocation, not whole-interval exclusion (2026-07-05)](eval-metrics.md) (search: "## Lesson: Region WER buckets need word-level allocation, not whole-interval exclusion (2026-07-05)") - During M17 Phase 0, the first clean-vs-overlap WER split assigned an entire TextGrid interval or transcript row to the overlap bucket if it touched cross-talk at all.
- [Log-derived timelines must count state changes, not echoed decisions (2026-07-05)](eval-metrics.md) (search: "## Lesson: Log-derived timelines must count state changes, not echoed decisions (2026-07-05)") - During M16 diagnostics, the first role-timeline summary counted accepted flips whenever a timeline row had `decision == "accepted_flip"`.
- [Original fixture metrics outrank plausible seam fixes (2026-07-05)](eval-metrics.md) (search: "## Lesson: Original fixture metrics outrank plausible seam fixes (2026-07-05)") - A conservative speaker-anchor change for consultation-03 passed the focused `tests/python/test_nemo_session.py` suite and matched the local theory that tiny context-tail overlaps can trigger bad…
- [Held-tail speaker anchors need replay proof before adoption (2026-07-05)](eval-metrics.md) (search: "## Lesson: Held-tail speaker anchors need replay proof before adoption (2026-07-05)") - During M20 Phase 5, the per-window artifacts made held-tail speaker anchoring look like the small seam mechanism M16 had left open: c03 @83s had 18 held rows and wrong rows clustered at window…
- [Eval-generated session IDs must use route-valid UUIDs and avoid retry collisions (2026-07-05)](eval-metrics.md) (search: "## Lesson: Eval-generated session IDs must use route-valid UUIDs and avoid retry collisions (2026-07-05)") - During M17 channel-ceiling work, the first eval script posted human-readable session IDs such as `primock57-...-doctor` to `/transcribe/file`; FastAPI rejected them with `400 Bad Request` because the…
- [Persisted fixture commands must use unique slugs (2026-07-10)](eval-metrics.md) (search: "## Lesson: Persisted fixture commands must use unique slugs (2026-07-10)") - M02's three-run command used `consultation03`, which had been unique when the plan was drafted but matched four WAVs after the local corpus expanded.
- [Grace-bound actions run before diagnostic settle sleeps (2026-07-10)](eval-metrics.md) (search: "## Lesson: Grace-bound actions run before diagnostic settle sleeps (2026-07-10)") - Role-timeline instrumentation initially inserted an eight-second settle before the corrected-fixture runner requested post-visit correction.
- [Harness line-count failures after instruction edits (2026-07-04)](review-feedback.md) (search: "## Lesson: Harness line-count failures after instruction edits (2026-07-04)") - Adding required hot-path headings to `CLAUDE.md` fixed structural checks but pushed the file over the harness hard limit reported by `instruction-line-count`.
- [Bot "Addressed in <commit>" markers are not evidence](review-feedback.md) (search: "## Lesson: Bot") - Triaging the PR #3 bot reviews (Copilot, Codex, CodeRabbit), five of CodeRabbit's "✅ Addressed in commits ..." markers pointed at commits that never touched the flagged code.
- [Bot findings on long-lived PRs go stale, not wrong](review-feedback.md) (search: "## Lesson: Bot findings on long-lived PRs go stale, not wrong") - Two Codex cross-boundary findings on PR #3 ("live summaries overwrite the finalized transcript via replace_segments"; "stop closes the EventSource before the finalize tail") were INVALID at HEAD -…
- [Fixture CLIs should load helpers without mutating `sys.path`](tooling-gates.md) (search: "## Lesson: Fixture CLIs should load helpers without mutating `sys.path`") - The first corrected source-chip scorer CLI inserted `strands_agents` into `sys.path` so it could import the helper during local script execution.
- [Long runtime probes should become small scripts before execution](tooling-gates.md) (search: "## Lesson: Long runtime probes should become small scripts before execution") - During M03 post-visit timestamp work, an inline `docker compose exec` probe with a long embedded Python heredoc was blocked by the PreToolUse hook as too complex to review safely.
- [Semantic anchors should prefer function names over escaped route strings (2026-07-04)](tooling-gates.md) (search: "## Lesson: Semantic anchors should prefer function names over escaped route strings (2026-07-04)") - `./scripts/context-validate.sh` rejected a generated footgun citation that used an escaped decorator string for the WebSocket route in `strands_agents/api/server.py`.
- [Goat-flow installed skill edits can drift from package templates (2026-07-04)](tooling-gates.md) (search: "## Lesson: Goat-flow installed skill edits can drift from package templates (2026-07-04)") - Updating installed goat-plan skill copies under `.agents/skills/`, `.claude/skills/`, and `.github/skills/` removed stale project path text, but `goat-flow audit` still compares those files to the…
- [Gruff context docs need marker vocabulary (2026-07-04)](tooling-gates.md) (search: "## Lesson: Gruff context docs need marker vocabulary (2026-07-04)") - During M05, comments clearly described user-visible error handling but still failed `docs.missing-error-behavior-doc` because gruff's context-doc rule looks for marker words such as `reports`,…
- [Gruff env placeholders are exact-token sensitive (2026-07-04)](tooling-gates.md) (search: "## Lesson: Gruff env placeholders are exact-token sensitive (2026-07-04)") - During M06, intuitive placeholders such as `<generate-app-secret>` and `allowlists.secretPreviews` still left `sensitive-data.hardcoded-env-value` findings.
- [Gruff PHP display filters do not lower the exit threshold (2026-07-04)](tooling-gates.md) (search: "## Lesson: Gruff PHP display filters do not lower the exit threshold (2026-07-04)") - During M07, a complexity-only gruff-php command was initially considered for the retired cyclomatic alias.
- [Generic Gruff baseline filenames collide across tool lanes (2026-07-05)](tooling-gates.md) (search: "## Lesson: Generic Gruff baseline filenames collide across tool lanes (2026-07-05)") - During M14 review, a PHP accepted-debt baseline was written as `gruff-baseline.json`.
- [Validation wrappers must check exit codes before success text (2026-07-05)](tooling-gates.md) (search: "## Lesson: Validation wrappers must check exit codes before success text (2026-07-05)") - During M14 review, direct `composer validate --strict` exited 1 for the intentionally commit-pinned Strands PHP client, but preflight had been grepping for "is valid" and therefore reported the step…
- [SDK observability plans must match installed vendor contracts (2026-07-04)](tooling-gates.md) (search: "## Lesson: SDK observability plans must match installed vendor contracts (2026-07-04)") - During M08, the plan described PHP client 1.5.x `ResponseObserver` hooks, but the installed 1.4.0 client only exposes `RequestMiddleware` with `beforeRequest()` and `afterResponse()`.
- [Goat-flow setup-green can still hide cross-agent drift (2026-07-05)](tooling-gates.md) (search: "## Lesson: Goat-flow setup-green can still hide cross-agent drift (2026-07-05)") - During a Codex goat-flow 1.13.1 repair, `goat-flow setup . --agent codex` reported `0 audit checks failed` after codex config, hooks, and skills were synced.
- [Dataclass script imports need sys.modules registration (2026-07-04)](tooling-gates.md) (search: "## Lesson: Dataclass script imports need sys.modules registration (2026-07-04)") - Full pytest caught that the test helper loaded `scripts/analyze-logs.py` with `importlib.util.module_from_spec()` but did not register it in `sys.modules` before executing the module.
- [Provider probes must modify the SDK-formatted request in place](tooling-gates.md) (search: "## Lesson: Provider probes must modify the SDK-formatted request in place") - The first M08 Bedrock token-cap probe passed a new `inferenceConfig` beside the request generated by the installed Strands formatter.
- [GPU image import gates need a local/pending split (2026-07-04)](tooling-gates.md) (search: "## Lesson: GPU image import gates need a local/pending split (2026-07-04)") - During M09, the plan required a `docker run nvcr.io/nvidia/nemo:26.02 ...` import check before dependency edits.
- [Name GPU-pending fallbacks as fallbacks (2026-07-04)](tooling-gates.md) (search: "## Lesson: Name GPU-pending fallbacks as fallbacks (2026-07-04)") - During M11, the plan targeted NeMo decode-time phrase boosting, but the exact multitalker transducer API still needs a pinned-container GPU spike.
- [New ASR checkpoints need pinned-container proof (2026-07-06)](tooling-gates.md) (search: "## Lesson: New ASR checkpoints need pinned-container proof (2026-07-06)") - During the second-pass accuracy spike, `nvidia/parakeet-unified-en-0.6b` looked like the right newer English ASR candidate from the model card, but failed inside the pinned…
- [Gruff PHP file intent must precede the strict-types declaration](tooling-gates.md) (search: "## Lesson: Gruff PHP file intent must precede the strict-types declaration") - M01 added a documented test-only PHP router, but its file docblock followed `declare(strict_types=1)`.
- [Post-visit correction smoke tests must stay inside reconnect grace](verification.md) (search: "## Lesson: Post-visit correction smoke tests must stay inside reconnect grace") - A consult-03 browser smoke replay reached `02:57` and generated a summary, but the corrected transcript artifact was empty.
- [Editing agent source evicts in-memory sessions via uvicorn reload](verification.md) (search: "## Lesson: Editing agent source evicts in-memory sessions via uvicorn reload") - A summary-prompt edit to `strands_agents/agents/summary_agent.py` triggered the nemo-agent's uvicorn `--reload`, which restarted the process and wiped the in-memory `SessionStore` - the captured user…
- [Full-clip proportional word timings drift - timing splits need an internal coherence guard](verification.md) (search: "## Lesson: Full-clip proportional word timings drift - timing splits need an internal coherence guard") - The first runtime M08 eval run split the consult-08 age echo at 14.32-15.12 while the true doctor echo sits at ~16.1-16.6: the token-timestamps-proportional-to-words estimate that was ~200ms accurate…
- [Graceful client recovery can disguise a server crash as a clean stop (2026-07-05)](verification.md) (search: "## Lesson: Graceful client recovery can disguise a server crash as a clean stop (2026-07-05)") - A demo replay "stopped by itself" at ~70s and was signed off as the socket-drop recovery path working: the browser drained, showed "Replay stopped", and even produced good ground-truth metrics.
- [Config-echo checkmarks are not integration checks - invoke the dependency from its runtime (2026-07-05)](verification.md) (search: "## Lesson: Config-echo checkmarks are not integration checks - invoke the dependency from its runtime (2026-07-05)") - `./scripts/check-ai-model.sh` printed `✔ model us.anthropic.claude-haiku-4-5-20251001-v1:0` and `✔ AWS credentials resolve with STS`, so Bedrock looked healthy while every summary failed in ~140ms…
- [Guard shared socket handlers against stale-socket events (2026-07-05)](verification.md) (search: "## Lesson: Guard shared socket handlers against stale-socket events (2026-07-05)") - While converting demo replay to stream over the live WebSocket, the shared `onclose` handler gained a replay branch (`public/js/scribe-recording.js`, search: "A drop of the replay's own socket").
- [Verify the running container's env, not the compose default (2026-07-04)](verification.md) (search: "## Lesson: Verify the running container's env, not the compose default (2026-07-04)") - After changing the `docker-compose.yml` default to `OLLAMA_HOST=http://ollama:11434`, one `docker compose up -d` showed the agent with the fixed value, so the "agent can't reach Ollama" bug was…
- [Audio format mismatch - read both pipeline ends (2026-03-21)](verification.md) (search: "## Lesson: Audio format mismatch - read both pipeline ends (2026-03-21)") - AudioBuffer in `strands_agents/nemo_session.py` assumed 16 kHz 16-bit PCM while the browser MediaRecorder sent WebM/Opus.
- [Stale references after rename (2026-03-21)](verification.md) (search: "## Lesson: Stale references after rename (2026-03-21)") - After renaming `mercure_topic_raw` to `mercure_topic_segments`, stale references remained in config and docs.
- [Browser stream state ordering needs a focused regression check (2026-07-04)](verification.md) (search: "## Lesson: Browser stream state ordering needs a focused regression check (2026-07-04)") - git history (auto-seeded) Evidence: `templates/scribe/index.html.twig` + commit `0125a6b` fixed the StreamOrchestrator `_active` ordering bug.
- [Live transcription fixes need cross-layer verification, not one-service checks (2026-07-04)](verification.md) (search: "## Lesson: Live transcription fixes need cross-layer verification, not one-service checks (2026-07-04)") - git history (auto-seeded) Evidence: `docker-compose.yml`, `strands_agents/api/server.py`, `strands_agents/nemo_pipeline.py`, `strands_agents/nemo_session.py`, `templates/scribe/index.html.twig`, and…
- [Logging-only edits can still break hot role logic (2026-07-05)](verification.md) (search: "## Lesson: Logging-only edits can still break hot role logic (2026-07-05)") - During M18 Phase 0, a plain-log message was added to `RoleMappingState.did_update_mapping_detect_flip()` using `changed_speakers`, but that variable existed only inside `_is_role_label_flip()`.
- [Windowed-emission tests must clear the minimum-new-audio floor (2026-07-05)](verification.md) (search: "## Lesson: Windowed-emission tests must clear the minimum-new-audio floor (2026-07-05)") - During M17 fragment-policy work, a regression test intended to prove alternating-speaker fragments stayed separate put the first fragment's end exactly at the `_MIN_NEW_AUDIO_SECONDS` boundary.
- [Emission floors need inclusive boundary tests (2026-07-05)](verification.md) (search: "## Lesson: Emission floors need inclusive boundary tests (2026-07-05)") - During M17 seam fallback work, raising the context-replay floor from 0.3s to 0.5s used a strict `>` comparison.
- [Hidden sidebars need layout-state smoke tests (2026-07-04)](verification.md) (search: "## Lesson: Hidden sidebars need layout-state smoke tests (2026-07-04)") - During M12, the first clinical-hints UI pass hid the sidebar element but left a dedicated desktop grid column in the base layout.
- [Optional JSON knowledge files need malformed-file coverage (2026-07-04)](verification.md) (search: "## Lesson: Optional JSON knowledge files need malformed-file coverage (2026-07-04)") - During M12, the clinical KB loader handled missing files and invalid shapes but did not handle malformed JSON.
- [Model stack docs need env, Compose, and code defaults checked together (2026-07-04)](verification.md) (search: "## Lesson: Model stack docs need env, Compose, and code defaults checked together (2026-07-04)") - While creating the stack inventory, the root README still described Bedrock as the role-inference default, `.env.example` described Ollama as the local default, Compose passed Ollama by default, and…
- [Browser replay smokes need a trustworthy origin (2026-07-04)](verification.md) (search: "## Lesson: Browser replay smokes need a trustworthy origin (2026-07-04)") - While testing the Demo Audio picker, a Playwright smoke loaded the page on `http://app.test/`.
- [Populated transcript layouts need populated browser smokes (2026-07-04)](verification.md) (search: "## Lesson: Populated transcript layouts need populated browser smokes (2026-07-04)") - During the 0.3.0 mockup refresh, an empty-state screenshot made the new workspace layout look clean, but a populated transcript/summary browser smoke exposed that direct transcript events could leave…
- [Delivery-race claims need receiver-side evidence (2026-07-06)](verification.md) (search: "## Lesson: Delivery-race claims need receiver-side evidence (2026-07-06)") - Analyzing a manual replay from server logs alone, the agent saw the browser summary carry 38 segments while the server stored 40, saw the WebSocket disconnect timestamp precede the finalize-flush…
- [Agent-only QA artifacts must be fetched from the agent origin (2026-07-06)](verification.md) (search: "## Lesson: Agent-only QA artifacts must be fetched from the agent origin (2026-07-06)") - During corrected-transcript manual QA, the browser replay, correction, and summary all succeeded, but the Playwright artifact script failed with `Unexpected token '<'` after fetching…
- [Correction allocators need ASR-drop replay proof (2026-07-06)](verification.md) (search: "## Lesson: Correction allocators need ASR-drop replay proof (2026-07-06)") - The first anchor-allocation pass fixed opener/empathy rows and passed focused tests, but a fresh consult-03 browser replay exposed a dropped patient utterance: second-pass ASR omitted "and I just…
- [Full-page screenshot compression can misreport theme rendering](verification.md) (search: "## Lesson: Full-page screenshot compression can misreport theme rendering") - During summary UX M4, a downscaled full-page dark-mode screenshot made the summary panel look white-on-dark, suggesting the new tab CSS ignored the dark theme.
- [A block class rendered in two views breaks strict-mode e2e locators](verification.md) (search: "## Lesson: A block class rendered in two views breaks strict-mode e2e locators") - The M5 provenance popover deliberately reuses the Transcript tab's `.summary-transcript__block` builder so cited utterances look identical in both places.
- [pytest owns extra root log handlers; assert on the app's handler only](verification.md) (search: "## Lesson: pytest owns extra root log handlers; assert on the app's handler only") - While regression-testing the correlation-ID fix (filter moved from the root logger to the root handler, `strands_agents/api/server.py`, search: "CorrelationIdFilter"), a test asserted that EVERY…
- [Dev-tooling transitives mask undeclared runtime imports](verification.md) (search: "## Lesson: Dev-tooling transitives mask undeclared runtime imports") - A post-hardening Codex P1 caught what the local test suite could not: `strands_agents/api/mercure_publisher.py` (search: "import jwt as pyjwt") minted the publisher token on the DEFAULT compose path,…
- ["Peek instead of create" needs a liveness signal when the call is a write (2026-07-07)](verification.md) (search: '## Lesson: "Peek instead of create" needs a liveness signal when the call is a write (2026-07-07)') - Replacing `get_or_create_state` with `peek_state` in the speaker-scope role override (0.4.0 M04) silently broke a live-visit contract: a clinician can click a speaker label BEFORE the role worker has…
- [Environment-variable checks are not device-liveness checks (2026-07-08)](verification.md) (search: "## Lesson: Environment-variable checks are not device-liveness checks (2026-07-08)") - The M06 phase-2 gate followed the standing rule "verify the RUNNING container env, not compose defaults" - `NEMO_SESSION_ENGINE=streaming` and `MEDICAL_BOOST_ENABLED=1` were confirmed in the…
- [Styling-carried UI state does not survive text paste - read fidelity and correction outcomes from logs](verification.md) (search: "## Lesson: Styling-carried UI state does not survive text paste - read fidelity and correction outcomes from logs") - During the 2026-07-08 (UTC) manual round, a pasted c03 note showed no "Unverified against transcript" markers and the review concluded "zero flags fired".
- [Field-session fixture replays must preserve the original stop time](verification.md) (search: "## Lesson: Field-session fixture replays must preserve the original stop time") - M11 needed fresh rows for a field note captured 228 seconds into a 510-second day3 WAV.
- [Long evals must capture rotating service logs during the run](verification.md) (search: "## Lesson: Long evals must capture rotating service logs during the run") - The first 20-fixture full-corpus sweep waited until the end to inspect Docker logs, but rotation had already discarded 13 of 15 early `correction.completed` lines.
- [Source-based shell smokes need the main guard before the first run](verification.md) (search: "## Lesson: Source-based shell smokes need the main guard before the first run") - M01's first GPU-free corpus smoke sourced the existing eval runner before a library guard existed, so sourcing immediately entered the real fixture main path.
