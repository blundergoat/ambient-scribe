# Pytest config for Ambient Scribe Python tests.
# It keeps async tests explicit so user-facing session, WebSocket, and queue
# flows do not pass by accidentally sharing event loops between tests.
[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
