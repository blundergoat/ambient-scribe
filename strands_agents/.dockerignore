.venv/
__pycache__/
*.pyc

# Runtime output, never build input. session-evidence/ holds complete transcript
# and clinical-note wording for real consultations, and quality/ holds evaluation
# artifacts; COPY . /app would otherwise bake both into a shared image layer.
var/
